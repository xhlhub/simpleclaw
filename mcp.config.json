{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/Users/mac"
      ],
      "description": "文件系统操作（读写、重命名、搜索等）"
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"],
      "description": "浏览器自动化（导航、点击、截图、表单填写等）"
    },
    "shell": {
      "command": "npx",
      "args": ["-y", "mcp-shell"],
      "description": "Shell 命令执行（支持 ssh、scp、rsync 等远程操作）"
    },
    "xiaohongshu MCP": {
      "command": "/Users/mac/Documents/code/Redbook-Search-Comment-MCP2.0/start.sh",
      "args": []
    }
  }
}
